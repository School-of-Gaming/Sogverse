-- A comment describes current behaviour, never a migration number.
--
-- WHAT THIS IS FOR
--
-- Sixty-nine object comments in `public` narrated their own history by
-- migration number: "since 00171 it also ...", "renamed from X in 00175",
-- "00206's lesson, one system over". The owner's ruling is that a comment
-- describes what the object does now and why; which day it started doing it is
-- git's business. The root CLAUDE.md carries the rule and a unit test sweeps
-- the repo for the citations, but a comment on a database object is not text a
-- sweep can fix: it lives in pg_description, the schema dump only reports it,
-- and the only way to change it is to re-issue COMMENT ON. That is this
-- migration.
--
-- WHAT CHANGED IN EACH COMMENT
--
-- Every fact about what the object does, and every reason it does it that way,
-- is kept verbatim where the sentence allowed and reworded into the present
-- tense where it did not. What is dropped is the changelog and nothing else:
-- "since <number>", "<number> replaced <thing>", "before <number> it ...", and
-- bare parenthetical citations. Dated owner rulings and references to entries
-- under docs/ are kept — they name a source a reader can still follow, which is
-- exactly what a migration number stopped being once the numbered history was
-- squashed into a baseline.
--
-- WHAT IS DELIBERATELY LEFT ALONE
--
-- Migration numbers also appear in the inline `--` comments inside function
-- BODIES. Those can only change by redefining the function, which would mean
-- re-emitting thirty function bodies — the one schema change two branches
-- cannot merge — for a comment edit. They are left for the owner to decide on
-- separately, and the assertion at the end of this file is scoped accordingly:
-- it reads pg_description, which holds object comments only, so a body comment
-- is outside what it can see and outside what it claims.
--
-- No grants, no search_path: COMMENT ON resolves the object from the name given
-- and writes nothing a function body could be tricked into reading.

COMMENT ON FUNCTION public.admin_enroll_participant(p_product_id uuid, p_participant_id uuid) IS 'Admin-gated comp-enrollment: drops a participant onto a product with status=active, bypassing payment, seat caps and registration windows by design. Refuses only a paid consumer club — the one shape whose seat requires a Stripe subscription this function cannot create; free clubs enroll like any free camp or event. It enforces the audience: a customer profile takes a seat as their own customer and needs for_parents, anyone else is resolved through the parent link and needs for_gamers. It places the seat automatically when the product charges nothing (billing_mode free or external_contract) AND has exactly one group, matching the family self-enrollment path; a PAID camp or event lands in the unassigned inbox, as does any product with zero or several groups, and whether the single group has a gedu assigned is not consulted. That placement is why the product read takes FOR UPDATE — the group count has to be taken under the same lock the group editor holds. group_joined_at is never written here; a trigger stamps it from group_id. It is the THIRD door into record_required_consents, and the only one that neither prompts nor refuses: admins are trusted, a comp-enrollment is arranged with the family off-platform, so every slug in the product''s requirement set is supplied automatically and the acceptance rows are written on the family''s behalf — customer_id the family''s, accepted_by the acting admin''s auth.uid(). It runs AFTER the INSERT because the partial unique index is the already-enrolled gate on this path, so an acceptance exists only where a seat does. There is no consent argument on this function or on the route above it: nothing about the Add button''s behaviour differs on a consent-requiring product.';

COMMENT ON FUNCTION public.admin_move_participation(p_participation_id uuid, p_target_product_id uuid, p_stripe_price_id text, p_expected_source_product_id uuid, p_expected_stripe_price_id text, p_group_id uuid) IS 'Admin-gated club switch, database half: repoints one ACTIVE, SUBSCRIBED participation at another paid consumer club and stamps its family_subscriptions row with the price id the route has already moved the Stripe subscription onto. Guard-first on assert_admin. Locks BOTH product rows in one statement ordered by id, which is what stops two admins switching in opposite directions from deadlocking. Eight refusals, each with its own errcode so the route can map them: the participation or the target product does not exist (no_data_found); the target IS the source, the seat is not active, the target is not a paid subscription-shaped club, p_group_id names a group that is not the target''s, the seat is no longer on p_expected_source_product_id (the seat has moved) or the live subscription no longer carries p_expected_stripe_price_id (the price has changed) (check_violation); the seat carries no live subscription — a family_subscriptions row whose status is anything but cancelled, the same predicate admin_remove_participation refuses ON (object_not_in_prerequisite_state). The two expected-state arguments bind the write to what the commit route actually read: Stripe replays a reused idempotency key without touching the subscription, so a stale dialog can believe it moved a price it did not, and the seat would otherwise be moved under a subscription billing for another club. p_group_id is the admin''s placement on the target, chosen in the same dialog as the move so the seat does not have to be dragged out of the unassigned inbox afterwards; omitted or NULL, placement falls to the shared automatic-placement rule applied to the target, which for a paid target is always that inbox. The join instant is never written here — the BEFORE UPDATE trigger stamps it from group_id. Seat cap, age range, region lock, product status and required consents are deliberately NOT enforced: admins are trusted and the panel raises each as a picker warning, in the posture it already takes when an admin promotes over capacity. The already-on-the-target collision is not pre-checked either — the partial unique index over active/waitlisted/completed rows raises 23505 and the commit route pre-flights it with a plain read before touching Stripe. The row is UPDATEd rather than deleted and recreated, so the subscription row''s unique cascading foreign key and the seat''s payment marker travel with it; the purchase timestamp and the checkout session are untouched.';

COMMENT ON FUNCTION public.admin_set_product_gamer_photo_consents(p_product_id uuid, p_consent_types public.gamer_photo_consent_type[]) IS 'Replace the set of photo consents a product''s signup panel asks about — and therefore whether its session editor shows a gedu the roster''s photo permissions at all — admin-only and guard-first on assert_admin. The only writer of product_gamer_photo_consents: that table carries no write grant for any Data API role, and an inline INSERT from the admin product form would need one, because the form reaches this as the admin''s own session role. NULL and an empty array both mean "asks nothing", which is how a set is cleared. A NULL ELEMENT is refused before the replacing DELETE runs, for the reason two systems over refuse one: `NOT (col = ANY (array))` is three-valued, so a NULL inside the array would match nothing and turn the wipe-and-replace into a merge. An unknown product is refused explicitly rather than by a foreign key, because a call that CLEARS the set performs no insert for an FK to fire on and would otherwise report success for a product that does not exist. The exact twin of admin_set_product_marketing_consents.';

COMMENT ON FUNCTION public.admin_set_product_marketing_consents(p_product_id uuid, p_consent_types public.marketing_consent_type[]) IS 'Replace the set of marketing consents a product''s signup panel asks about, admin-only and guard-first on assert_admin. The only writer of product_marketing_consents: that table carries no write grant for any Data API role, and an inline INSERT from the admin product form would need one, because the form reaches this as the admin''s own session role. NULL and an empty array both mean "asks nothing", which is how a set is cleared. A NULL ELEMENT is refused before the replacing DELETE runs, for the reason one system over refuses one: `NOT (col = ANY (array))` is three-valued, so a NULL inside the array would match nothing and turn the wipe-and-replace into a merge. An unknown product is refused explicitly rather than by a foreign key, because a call that CLEARS the set performs no insert for an FK to fire on and would otherwise report success for a product that does not exist.';

COMMENT ON FUNCTION public.apply_group_changes(p_product_id uuid, p_added_groups jsonb, p_renamed_groups jsonb, p_deleted_group_ids uuid[], p_gedu_assignments_added jsonb, p_gedu_assignments_removed jsonb, p_participation_moves jsonb) IS 'The admin groups panel''s whole batch, applied in one transaction: remove assignments, delete groups, rename groups, add groups (each with its educators inline), add assignments, and move participations between groups. Admin-only, guard-first, and it takes the PRODUCT row''s lock first so two admins editing one product''s groups serialize rather than interleave. Removes run BEFORE adds so moving an educator from group A to group B is one batch — the (gedu_id, product_id) UNIQUE would otherwise refuse the add. Newly added groups are addressed by a client-minted `tempId` and the returned `tempMap` hands back the real ids, which is what lets one batch create a group and move members into it. An assignment carries a ROLE: an added assignment element is { groupId, geduId, role } and upserts ON CONFLICT (group_id, gedu_id) DO UPDATE SET role, so a role change is ONE add rather than a remove plus an add — which also means re-adding an existing pair is not a no-op, it restates the role. An added GROUP''s educators arrive as gedus: [{ geduId, role }]; the legacy geduIds array of bare ids is still read for the deploy window and lands every one of them as a primary. An omitted role is a primary, which is also the column''s default. This function is DELIBERATELY ASSIGNMENT-ONLY with respect to session substitutions, and is annotated as such in the completeness check: it is the writer of the permanent relationship, not a gate on it. Removing an assignment also sweeps the substitution requests it orphans: for every date the removed gedu held a live request on, the same fixpoint every other unseating runs. It remains ASSIGNMENT-ONLY as a GATE — it still gates on nothing and still writes no substitution row — but a writer that can unseat somebody has to leave the derivation consistent, or an admin could answer a request filed by a person who is no longer expected at the session.';

COMMENT ON FUNCTION public.apply_product_image_path() IS 'BEFORE INSERT OR UPDATE on products: image_path is derived from the linked product_images entry, and is NULL whenever image_id is. No branch preserves an app-supplied path, so this function is the column''s ONLY writer — which is what carries the invariant that a served path is a catalogue path, and why there is deliberately no foreign key on image_path (a second relationship between products and product_images makes every PostgREST embed ambiguous). Carries no column list on the trigger deliberately, so no statement can name image_path and win.';

COMMENT ON FUNCTION public.approve_session_substitution_offer(p_offer_id uuid) IS 'An admin picks one offer, and its gedu becomes the substitute: `open` -> `substituted`, stamping substitute_id, approved_by = the acting admin, and approved_at. Takes the group row''s lock and then the request''s FOR UPDATE, so two admins approving two offers on one session serialize and the second is refused; and re-asks gedu_may_substitute_session UNDER THAT LOCK, because an offer goes stale (the offerer gets assigned to the group, gets seated as somebody else''s sub on the same date, files an absence of their own, or is de-certified). The OTHER OFFERS ARE NOT TOUCHED: "not selected" is derived from the request being substituted by somebody else, and which offer was approved is the substituting gedu''s own offer row — which is why no approved_offer_id exists. It also re-asks, under the lock it already takes, whether the ABSENT gedu still holds a seat at the session (gedu_holds_seat_at_session): an admin can remove a gedu from the group through the groups panel while a request of theirs is open, and approving an offer on an orphaned request would seat a sub to substitute for nobody and hand them the group''s workspace for it. A refusal rather than a withdraw-and-refuse, because the RAISE would roll a withdraw back with the rest of the transaction.';

COMMENT ON FUNCTION public.cascade_withdraw_orphaned_substitution_requests(p_group_id uuid, p_session_date date) IS 'Internal: after an admin edit that can unseat somebody — clear, withdraw, or the replace inside set_session_substitution — withdraw every non-withdrawn request on that (group, date) whose requester no longer holds a seat there, meaning neither assigned nor the substitute_id of a live substituted request. A FIXPOINT sweep rather than one statement, because unseating cascades: clearing X''s substitution withdraws X''s own request, which unseats whoever was substituting that, and so on until a pass changes nothing. Withdrawing a substituted row blanks its three substitution columns — chk_substitution_state forbids a withdrawn row from carrying a sub — which is deliberate: an admin unwinding a mistake leaves no phantom substitution behind for invoicing to bill. Withdrawing a request whose requester was meanwhile unassigned restores nobody and is allowed. Not granted to `authenticated`; called only from inside the admin RPCs. "No longer holds a seat" is gedu_holds_seat_at_session, one predicate rather than two inline NOT EXISTS clauses. apply_group_changes is among the callers: removing a gedu from a group through the admin groups panel unseats them without touching a substitution row, so that writer sweeps every date the removed gedu held a live request on.';

COMMENT ON FUNCTION public.claim_expired_seat_offer_notifications(p_participation_id uuid) IS 'Claim seat offers that have run out unanswered and have not been reported to staff, and return what the mails need. One data-modifying CTE does both halves, which is what makes the notification exactly-once under concurrency: a second sweep re-evaluates seat_offer_expiry_notified_at IS NULL after the first commits and claims nothing, with no advisory lock and nothing held across the send. TWO MODES, and the argument is a security boundary rather than an optimisation. p_participation_id NULL sweeps the whole platform and is what the ADMIN surfaces pass — an admin opening the dashboard or a groups panel is entitled to observe every lapsed offer. A non-NULL id claims that row and nothing else, and is what every FAMILY-triggered observation passes: the emailed link is a signed token naming exactly one participation whose signature never expires, so unscoped it was a permanent unthrottled trigger for a platform-wide write; scoped, the worst a leaked link can do is claim the notification for the row it already names. The in-app answer passes its own id on the same rule — a credential that names one row may only claim that row. There is deliberately no cron job — expiry is OBSERVED rather than scheduled. SILENCE COSTS THE PLACE IN LINE: the same statement re-stamps waitlisted_at with clock_timestamp(), moving each claimed family to the back of the queue, because a turn that came up and was not taken must not be offered first again while everybody behind waits another round. clock_timestamp() rather than now(), the rule for every cross-transaction ordering key — waitlisted_at orders rows against each other, and a sweep claiming several rows in one frozen transaction time would stamp them all identically. The two offer stamps are left alone: seat_offer_sent_at is what the emailed token still compares against (a late decline keeps working, and the landing page tells an expired link from a used one by that value) and the notified stamp is what makes this claim exactly-once. The claimed rows stay waitlisted, so the offer is still re-offerable and a second silence notifies again. Service-role only; the route establishes who is calling.';

COMMENT ON FUNCTION public.claim_group_session_report_email(p_group_id uuid, p_session_date date) IS 'Claim the one send of a session report to the group''s families, and hand back the row it claimed. Open to an ADMIN or to the gedu assigned to the group, exactly as the session-notes writer is. Takes the row''s lock, then refuses with SQLSTATE P0021 when there is no report to send (no row, or a report that is empty after the same whitespace trim the summaries SQL applies) and with P0022 when report_emailed_at is already set — both bind an admin identically; otherwise stamps report_emailed_at = now() and report_emailed_by = auth.uid(). The claim is the FIRST write of the send and is also its authorization: succeeding proves the caller may send for this group, which is what lets the route resolve recipients with the service role afterwards. Releasing a claim is the route''s job and happens only when every single mail failed. The assignment half is spelled out INLINE here instead of calling gedu_teaches_group, and that is a security decision rather than a convenience: gedu_teaches_group admits a live substitution on ANY of the group''s dates, while this mail is at-most-once with no resend, so a sub must not be able to send the families a write-up of a session they did not run. The substitution arm here is therefore DATE-SCOPED to the session being claimed — one of exactly two places on this surface that is, the other being the voice room.';

COMMENT ON FUNCTION public.create_participation(p_product_id uuid, p_participant_id uuid, p_customer_id uuid, p_purchase_shape text, p_currency text, p_consented_documents text[]) IS 'The family self-enrollment gate: validates one signup against the product (audience, effective status, registration window, currency, purchase shape, duplicate seat, seat cap) and then either writes the seat or reports that the caller may go and take the money. The two no-charge shapes — free and external (municipality, invoiced off-platform) — insert an active row here and now; the paid shapes return kind=''validated'' and nothing is written until confirm_paid_participation runs from the Stripe webhook, so an abandoned Checkout leaves nothing behind. Holds the product row FOR UPDATE from its first statement, which is what makes the seat-cap count and the group read below race-free against a concurrent signup or group edit. The two instant-active branches place the seat automatically when the product charges nothing AND has exactly one group: that combination has no placement decision left in it, so the row lands in that group rather than in the unassigned inbox. Zero groups, two or more groups, or any paid product land group_id NULL — the inbox — and whether the single group has a gedu assigned is not consulted. group_joined_at is never written here; a trigger stamps it from group_id. It takes p_consented_documents and, just after the seat-cap gate, calls record_required_consents: an enrolment onto a product with required consent documents is refused with check_violation unless the array covers all of them, and otherwise records one acceptance row per required document at that document''s current version. That runs for EVERY purchase shape — the paid ones write no participation row here, but the parent agreed here, so the record is made here, and an acceptance behind an abandoned Checkout is a harmless true statement. A full product returns kind=''full'' before any of it, because nobody enrolled. It names p_customer_id as the acceptance''s accepted_by as well as its customer: on this path the parent ticked the boxes themselves, which is what tells these rows apart from the ones admin_enroll_participant writes. service_role only: this function has no auth.uid() and trusts the calling route to have pinned p_customer_id to the session user.';

COMMENT ON FUNCTION public.create_product(p_product_type public.product_type, p_billing_mode public.billing_mode, p_translations jsonb, p_topic public.product_topic, p_spoken_language_code public.spoken_language, p_is_remote boolean, p_timezone text, p_registration_opens_at timestamp with time zone, p_for_gamers boolean, p_for_parents boolean, p_min_age integer, p_max_age integer, p_is_visible boolean, p_waitlist_enabled boolean, p_location_id uuid, p_start_date date, p_end_date date, p_seat_count integer, p_schedule_slots jsonb, p_prices jsonb, p_primary_gedu_fee_cents integer, p_assistant_gedu_fee_cents integer, p_municipality_fee_cents integer, p_material_url text, p_tag public.product_tag, p_region_lock_country text, p_required_consent_slugs text[], p_requires_gamer_creations boolean, p_invoice_customer_id uuid) IS 'Admin-gated product create: the parent row plus its translations, schedule slots, prices, the staff-only material link and the consent documents enrolling on it requires. It takes NO status: a product''s lifecycle is derived from its dates, so there is nothing for a creating caller to choose and nothing stored for a later reader to mistake for a fact. p_start_date is defaulted like every other optional argument, but the column behind it is NOT NULL — an omitting caller is refused by the column rather than creating a product with no day it begins. SECURITY INVOKER — the assert_admin() first statement runs as the caller, which is also why assert_admin itself is granted to authenticated. p_for_gamers/p_for_parents are non-defaulted on purpose: a defaulted audience is one an omitting caller could set without meaning to. p_tag IS defaulted, and for the opposite reason: null is a legal value for a tag, no CHECK backstops it, and codegen cannot express an explicit null for a non-defaulted argument at all — so omission is how "untagged" reaches the column, and the required-nullable wire schema is what stops an accidental omission upstream. p_region_lock_country is defaulted for exactly that reason too, and carries one more thing worth knowing: the lock it writes is enforced in the UI alone, because a family''s location is self-attested — see the column comment. p_required_consent_slugs is defaulted on the same argument and is NOT written inline: this function is SECURITY INVOKER and product_required_consents carries no write grant, so the row goes through set_product_required_consents, the join table''s single guarded writer. p_requires_gamer_creations is defaulted to FALSE rather than to null, because the column is NOT NULL and false is the resting state of that whole feature — so an omitting caller creates an unflagged product, which is what omission should mean, and an explicit null is refused loudly by the column rather than silently becoming false. p_invoice_customer_id is defaulted on the same argument as p_tag — null is legal, nothing backstops its absence, and codegen cannot express an explicit null — and names the FENNOA CUSTOMER a municipality club is invoiced to; it is per club and never derived from a location, and chk_products_invoice_customer_only_for_muni refuses one on any other product type. This function does NOT take a picture: a product''s picture is the product_images entry its image_id points at, written by the route in a second statement, and the served image_path column is derived from that link by trg_products_apply_image_path. p_spoken_language_code is public.spoken_language, an enum rather than a text key into a reference table.';

COMMENT ON FUNCTION public.delete_group_session_image(p_image_id uuid) IS 'Remove one photo''s ROW from a session''s report. Open to an ADMIN or to ANY gedu assigned to the group — there is no per-photo ownership, matching how the report itself is edited under the last-editor model. Guard-first on assert_role; the group is then resolved from the image''s own session row, and that resolution is the second half of the gate. A photo id that belongs to another group and one that belongs to nothing are refused identically with 42501, so this cannot be used as an oracle for real photo ids. The route calls this LAST: it authorizes with assert_can_delete_session_image, removes the OBJECT through the Storage API (never with SQL against storage.objects, which orphans the backing file), and only then deletes the row here — so that a removal which failed to remove the picture leaves the photo on the card, visible and retryable, instead of taking the tile away while the object stands in a public bucket. This function''s own guard is not replaced by that check; it runs again on the actual delete. A row that survives a failed delete after its object is gone renders as a broken thumbnail, and the ordinary remove control is its repair: the storage API answers a delete of an absent object as success, so the retry reaches here and clears the row.';

COMMENT ON FUNCTION public.gedu_is_expected_at_session(p_gedu_id uuid, p_group_id uuid, p_session_date date) IS 'Internal predicate, and the derivation the whole feature rests on: a gedu is expected at (group, date) iff they hold no non-withdrawn request for it, AND they are either assigned to the group or hold a `substituted` request for it. That one sentence handles the sub-of-sub chain, an open sub-of-sub request and a cleared substitution alike, which is why nothing stores "who is running this session" anywhere. Takes the gedu as an argument rather than reading auth.uid(), because every writer asks it about somebody else. Deliberately makes NO access-window and NO certification test: it answers who is DOING THE JOB, a staffing fact, where gedu_substitutes_session answers who may REACH the group, an access fact — conflating them would make a past session''s staffing silently change fifteen days later. Where a gedu is both assigned and holds a substitution on the same group (only an admin edit can produce that), the assignment supplies the role. Not granted to `authenticated`. The second half of that sentence — "assigned to the group, or holding a substituted request for it" — is gedu_holds_seat_at_session, which this composes rather than restates: three callers ask that question, and it is written out once.';

COMMENT ON FUNCTION public.gedu_teaches_group_product(p_group_id uuid) IS 'Internal predicate: is the caller a gedu assigned to ANY group of this group''s product? The same question gedu_teaches_group asks, widened from one group to the whole product — which is the cross-group mobility the member-flair RPCs need, because a substitute standing in for another group is exactly the person who needs the note. Gedu-only and composed with is_admin() at each call site, the dominant pattern in this schema. NOT exposed to authenticated: it is called from inside SECURITY DEFINER RPCs and from nowhere else — in particular from no RLS policy, which is what lets it stay private, since a policy predicate is evaluated as the querying role and would have forced a grant. is_voice_group_moderator computes the same thing with is_admin() folded in; it is deliberately left alone rather than reused or renamed, because the voice_zones and voice_private_zone_occupants policies reference it and its name would make a note read look like a voice concern. It additionally admits a LIVE SUBSTITUTION on the group, and the substitution arm is deliberately GROUP-only rather than product-wide: an assignment is a standing relationship with a product, which is what earns the cross-group mobility above, while a substitution is one date on one group, and widening it to the product would hand a sub the member flair of every sibling group they were never asked to stand in for.';

COMMENT ON FUNCTION public.get_admin_dashboard() IS 'The whole admin dashboard in one document: per-role user counts (email-verified and, for gedus, certified — either can be NULL, where the stat has no meaning: certified only means something for an educator, and verified is NULL for a role none of whose accounts holds a real address, which is every gamer unless their parent chose sign-in mode email), the uncertified-gedu queue, live products carrying at least one ops issue, and the calendar facts the schedule and coming-up feed resolve weeks from. Admin-only, guard-first on assert_admin. Each queue candidate also carries contract_accepted_at — when they accepted the current gedu contract, or NULL — which informs the certification decision without gating it; that standing is judged on the version''s BASE, so either equally binding language of the current version counts, and a candidate holding both carries the earlier of the two signatures. Each candidate additionally carries criminal_record_check_at — when an admin recorded seeing their criminal record extract, or NULL — which informs the same decision on the same terms and gates nothing either; the flag beside it is not shipped because the stamp is non-NULL exactly when the flag is true. The waitlist attention item asks whether there is something for an admin to DO rather than what state the product is in: an open seat that already carries a live seat offer is subtracted, so a product whose every open seat has been offered drops out of the queue, and a decline or an expiry raises it again on its own. The count rides in the emitted object as live_offer_count so the page can explain the absence. An unstaffed group with NO active member is named too, in its own empty_groups_without_gedu array beside groups_without_gedu, and can put a product in the queue by itself: an admin pre-building next term has not made a mistake, and that reasoning decides the item''s RANK on the page rather than hiding it. The two arrays are disjoint by construction and neither holds a group somebody is assigned to. Both product sections ask effective_status() and nothing else: there is no stored status to pre-filter on, so the derived answer is the only lifecycle test either candidate set makes, and every date window is computed in the product''s own timezone. Each attention candidate also carries missing_invoice_customer — municipality clubs only, true when the club names no Fennoa invoice customer, and false everywhere else by construction because the CHECK forbids the column on any other product type. It puts a club in the queue on its own, exactly as the municipality fee beside it does: the link is nullable because a club is created before anybody has agreed who pays for it, and by the time it starts both the fee and the buyer are meant to be set, so a club still missing one is an admin omission rather than an ordinary state. The two are the bottom of the page''s ranking and sit together there, because both are a blank field on one form with one consequence — a month of invoices that cannot be written for that buyer. Product names are shipped as the whole product_translations array because which one to read is a property of the reader, exactly as every other admin surface treats them.';

COMMENT ON FUNCTION public.get_admin_municipality_invoicing(p_month_start date) IS 'One calendar month of municipality-club invoicing, as a single document: every municipality club that either recorded a session in the month or could have (a term that overlaps it), each with its timezone, term dates, current municipality_fee_cents, the whole product_translations array, its weekly schedule slots, its own location row, the nearest ancestor-or-self location of type municipality, the FENNOA INVOICE CUSTOMER it is billed to, and every stored group_sessions row in the month as a raw (group_id, session_date) pair. Admin-only, guard-first on assert_admin, and deliberately not STRICT so the guard cannot be skipped on NULL input. p_month_start must be the first day of a month; anything else raises check_violation. A session RAN iff a group_sessions row exists — those rows are lazily materialized when an educator records a report, note or attendance, so a row is the evidence somebody was there — and the client counts one session per club per calendar date, because a club with two groups meeting on one day ran one session. The schedule ships so the client can project what was supposed to run and flag a scheduled date with no row; a projection is never counted and never billed, and a stored row on an unscheduled date still counts. The candidate test is the TERM alone and the document carries no lifecycle column: also demanding a stored running or completed state would drop every club that recorded no session out of the invoice, which is exactly the club a missed-session flag exists to raise. The municipality walk climbs parent_id THROUGH retired rows and never filters them, because a school that has since closed still sat in its municipality. Every club in the document HAS a municipality: a club whose chain reaches none cannot be invoiced to anybody, so the whole read raises check_violation naming those product ids rather than shipping a null the caller would have to render somewhere outside every total. Each club also carries invoice_customer — the WHOLE customer row (number, invoice name, address, optional reference and invoice text) rather than an id, because the caller turns it into a Finvoice file — or null where nobody has said who pays yet. A null customer is NOT refused, unlike a null municipality: such a club renders on the page perfectly well and only its own file is blocked, so refusing the month would take every other file down with it. The link is the club''s own column and is never derived from its location, because one city can be two customers and an association can buy clubs sited in a municipality it is not. The fee is the current column value with no snapshotting, and NULL means unset — the client shows that as a blank to fix, never as zero. Every array ships as [] rather than null.';

COMMENT ON FUNCTION public.get_admin_product_sessions(p_product_id uuid) IS 'One round trip behind the admin product page''s Sessions panel: the product''s schedule parameters, its venue and site notes on an in-person product, and every group on it with its standing notes, its register roster and every stored session row with a sparse attendance map and its photos. Admin-only, guard-first on assert_admin. Product-keyed rather than group-keyed because the page shows one product and puts a group selector in front of the feed; asking per group would send the product shell and the site over the wire once per group. Contains no schedule expansion — the client owns the calendar math, exactly as it does for the gedu feed. The SESSION shape is get_gedu_group_feed''s verbatim, because one card component renders both and the two must not disagree about what a session is — which is why `images` ({id, width, height} per photo, ordered by (created_at, id), never the uploader) arrives here in the same shape and needs no versioned name: this document''s reader shares the gedu session''s tolerant schema, and only the strict family one needed get_my_family_product_feed_v2. The ROSTER deliberately is not the gedu feed''s — it carries participant_id and first_name alone, since the only thing this surface does with it is take the register, and the groups panel on the same page already answers who these people are. Each group additionally carries `gedus` ({id, first_name, role}) and `substitutions` (every non-withdrawn substitution request on the group), both byte for byte get_gedu_group_feed''s — one card component renders the session cards of both documents, and the staffing line and the Set-a-sub / Clear-sub / Withdraw-request editor are drawn from exactly these two arrays. `reason` and `reason_note` DO travel here: this document is admin-only end to end, and the reason is what the editor shows beside the request.';

COMMENT ON FUNCTION public.get_gedu_assigned_product(p_product_id uuid, p_group_id uuid) IS 'One round trip for a gedu opening a product they have a seat on: the product shell, which group is theirs, and every group on the product with its participant_count and gedus. The roster rides only on the caller''s own group and is keyed by participant_id — the same shape get_gedu_group_feed serves, kept in parity on purpose even though the rendered roster always comes from the feed''s fresher copy. The shell carries the product''s topic (which decides whether a game identity is shown at all, and which one) and each roster entry carries roblox_username/roblox_user_id beside the Minecraft pair. Each roster entry also carries the staff-only flair — group_joined_at, note and note_updated_by_first_name — emitted unconditionally, because the join stamp is a fact and the clubs-only newcomer rule is applied by the client. Each roster entry carries `creations` too (always an array, [] when there is no row) and the shell carries requires_gamer_creations, both in parity with get_gedu_group_feed for the same reason every other field is: the page composes both documents. THIS IS THE DOOR A SUBSTITUTION COMES THROUGH, and both halves of it are widened rather than only the gate: p_group_id is optional and, when given, must name a group of this product that the caller is assigned to or substitutes on — which is what lets a gedu substituting a SIBLING group of a product they already teach land in the right workspace instead of their own group''s. Without it the assignment group is resolved, and a caller with no assignment on the product falls back to their substituted group, deterministically ordered. A caller with neither is refused with 42501. Each entry of `gedus` additionally carries the assignment `role`.';

COMMENT ON FUNCTION public.get_gedu_group_feed(p_group_id uuid) IS 'One round trip for a group workspace: product shell (with the gedu-only material link, read from product_staff_details), group notes, site notes on in-person products, the current roster, and every stored session row with its sparse attendance map and its photos. Contains no schedule expansion — the client owns the calendar math. Open to an ADMIN as well as to the assigned gedu, guard-first on assert_role with the ownership question as a second 42501 — the same shape set_group_notes uses. The admin caller is the product page''s per-group GROUP DETAILS page, which renders the gedu workspace''s page body unchanged: one body fed by one document is what keeps the two surfaces one surface, where a second admin-shaped RPC would have started drifting field by field. An admin passes the ownership half outright; a gedu is shown only their OWN group''s feed, and a customer or a gamer is refused on the first statement, which is what keeps the material link and the three staff notes off every family surface. Each roster row is keyed by participant_id (whoever holds the seat, child or adult), carries both game identities (minecraft_username/minecraft_uuid and roblox_username/roblox_user_id, independent of each other and drawn according to the product''s topic, which this document does not carry), and carries two contact fields and never both: parent_email for a child (their linked parent), participant_email for an adult seat (their own address, NULL on child rows because a gamer profile''s email is a synthetic non-mailbox). Each roster row also carries the staff-only flair — group_joined_at (when the seat entered THIS group, as against signed_up_at, which is when it was taken on the product), note and note_updated_by_first_name — in deliberate parity with get_gedu_assigned_product''s roster, which is the parity the page depends on because it renders this copy. Each roster row additionally carries `creations` (always an array, [] when there is no row) — the one roster field that is NOT staff-only, since the member''s own family reads the same list — and the product shell carries requires_gamer_creations, because the final session''s fourth completeness condition is derived on the CLIENT from that flag, the schedule and this roster''s creations; no document carries an "owed" field. Each session row carries report_emailed_at — when its report was mailed to the families, NULL until it was — and never report_emailed_by, which is audit and renders nowhere. Each session row also carries `images`: {id, width, height} per photo, ordered by (created_at, id), with the uploader deliberately off the wire for the same reason the sender is. The key sits directly on the session rather than under a versioned name, because the gedu contracts schema is tolerant of unknown keys. The document carries two more members, both of them inputs to the client-side staffing derivation rather than answers from it: `gedus`, the group''s assignments as {id, first_name, role}; and `substitutions`, every NON-WITHDRAWN substitution request on the group, unbounded, in the one shape substitution_request_document defines and every substitution write returns. Withdrawn is the one status that does not travel, because it changes nothing about who is expected. The client merges substitutions onto its entries by date, and a projected date with no session row carries its requests like any other. `reason` and `reason_note` ride for an ADMIN caller only — this document is served to an admin too, so the flag is the CALLER''s role rather than a property of the RPC, which is what keeps a `sick` category off a colleague''s screen while the one document stays one document. `offer_count` rides for an admin and for the requester themselves. The GATE is gedu_teaches_group, which admits a live substitution on the group, so a sub opens the workspace they are substituting. It is the ONE caller that asks substitution_request_document to reveal the requester explicitly. The workspace is reached only by staff on the group and its session card''s staffing line names who is away; the REASON rides on the admin flag alone, so a colleague learns that somebody is absent and never that it was `sick`.';

COMMENT ON FUNCTION public.get_group_staff_overlay(p_group_id uuid) IS 'The staff-only marks for one group''s active roster, in one document: product_type, and a map keyed by participant id whose entries carry group_joined_at, note, note_updated_by_first_name and creations. Open to an ADMIN or to any gedu assigned to any group of the group''s product, guard-first on assert_role with the ownership question as a second 42501 — the same shape set_group_notes uses. Built for the voice room, which has no other route to these marks: staff-only data must never ride the Daily token or user_name, because that channel is broadcast to every peer including children. A refused caller means the flair is gated by data access rather than by a viewer prop. Note that `creations` is the one entry here that is NOT staff-only — the gamer''s own family reads the same list on their product page — but it rides this document because the per-gamer dialog is identical in every mount, including in-session. It is emitted as [] rather than null when there is no row, because a list has a real empty value where a note does not. product_type is on the document because the room knows only a group id, and the clubs-only newcomer rule is applied client-side from it. Every active member appears whether or not they have a note or a creation, so the map''s keys are the seat-holder set. An unknown group id returns a null-shaped document to an admin rather than raising.';

COMMENT ON FUNCTION public.get_my_assigned_products() IS 'Every product the calling gedu has a seat on, one row per seat, with the product shell, its schedule slots, how many groups it has and how many active seats (participant_count — a seat may be held by an adult as well as by a child). Gedu-gated on its first statement. TWO KINDS OF SEAT, discriminated by `kind`: an `assignment` row per gedu_group_assignments row, with `substitution_date` null; and a `substitution` row per UNEXPIRED substitution date, with `substitution_date` set — a `substituted` request whose holder is still certified and whose window has not closed, which is the whole of what gedu_holds_unexpired_substitution decides. That predicate rather than gedu_substitutes_session, and the difference is the point: this read draws the substitution CARD on My SOG, which stands from approval, where the workspace the card links to opens 48 hours before the substituted session. A substitution row therefore reaches a sub who cannot yet open the group, and carries nothing that would not be theirs to read then: names, a type, a date, the schedule, and two head counts. One RPC rather than two because the two kinds share every product-shell column and the dashboard card differs in its chrome rather than in the facts it needs.';

COMMENT ON FUNCTION public.get_my_family_product_feed(p_participation_id uuid) IS 'One round trip for a family club/camp/event page, scoped to ONE participation: the product shell, the group name and its family-facing note, the venue on in-person products, the teaching gedus'' first names, the group''s full stored session history with reports and PHOTOS, the named participant''s own attendance marks, and that participant''s own creations. Each session carries updated_by and the last editor''s first name — last editor of the SESSION, not author of the report: an attendance mark or a staff-note edit moves it. The name travels per session because a past session''s editor may no longer teach the group. Each session also carries `images`: {id, width, height} per photo, ordered by (created_at, id), the same shape the gedu and admin documents carry because one shared gallery renders all three, and never the uploader, which is safeguarding audit. That key rides on this strict family document under no versioned twin of its own, on the severity paragraph in docs/plans/CLAUDE.md: transient read-side breakage inside a release window is accepted. `creations` is a TOP-LEVEL array of {title, url} — this participant''s own, in this group, and structurally incapable of holding anybody else''s because it is not a map keyed by participant. It is the one staff-authored family-facing field that carries links, an owner-approved exception to the link-free rule session reports follow, and the render side parses each URL and degrades to the title as plain text when it is not http(s). Empty array when there is nothing, so the card renders on emptiness and never on null. Self-scoping — the caller must be the participation''s participant (a child, or a parent holding a seat of their own) or a parent linked to them; an unplaced participation has no page and answers P0002; a row that does not exist and a row belonging to another family are refused identically, so it cannot be used as an oracle for enrollment ids. Carries no gedu note of any scope, no roster, no other participant''s marks or creations, no parent email, no material link, no requires_gamer_creations flag and no owed/completeness state.';

COMMENT ON FUNCTION public.get_my_gedu_assignment_summaries(p_epoch_date date) IS 'One row per gedu assignment for the dashboard cards: group name, that group''s participant count (an active seat may be held by an adult as well as by a child), the venue name on in-person products, and how many past sessions still need attention. A finished session on or after the epoch counts until ALL of: the register is in, a family-facing report is written, the mail telling the families it is there has been sent, and — on the run''s FINAL session of a product with requires_gamer_creations set — every current roster member has at least one creation. The register condition is scoped to the members who had JOINED the group before that occurrence ended: both the marks counted and the size they are compared against, off participations.group_joined_at against an end instant resolved once per occurrence (the stored row''s ends_at, else the min slot end for that weekday, which is the same instant the "has it finished" test already used). group_participant_count and the empty-roster guard deliberately keep measuring the WHOLE current roster — a card''s headcount and the empty-group exemption are not per-occurrence questions. The report and mail conditions are unscoped because a session owes those whoever was in the room. The creations condition carries the SAME join-date scoping as the register condition, on the owner''s principle that a gedu owes a creation for every gamer who was in the group at the time of the last session — so a seat placed into the group after the final session ended owes nothing, and one occurrence cannot answer "who was this for" two different ways. Only the JOIN half of that principle is expressible: a member who has since LEFT owes nothing, because the roster is active seats and a departure leaves no trace. The final session is the last occurrence the schedule projects on or before end_date, derived here rather than stored; an open-ended product (end_date NULL) has none and therefore never owes creations, which is documented behaviour rather than an error. The badge''s unit is the SESSION: it counts sessions needing attention, and the final one simply has one more way to need it. The enforcement epoch travels in as an argument because it is a code constant, not a column. This count has a twin in TypeScript — the gedu feed''s entry-state derivation, which answers the same question for one card — and the two must be changed together, on all four conditions and on who a session is for, which scopes two of them. A SECOND KIND OF SEAT feeds the same machinery: a `substitution` row per substitution date, carrying `kind` and `substitution_date`, whose owed count is the same four conditions applied to a set of one occurrence — so it is 0 or 1 and never a term''s worth. That arm asks gedu_holds_unexpired_substitution rather than gedu_substitutes_session: the card stands from approval, where the workspace behind it opens 48 hours before the substituted session, and a card that waited for the workspace would hide from a sub the afternoon they had agreed to take. A substitution still locked owes nothing by construction, because every occurrence this count ranges over has already ended.';

COMMENT ON FUNCTION public.get_product_groups_with_details(p_product_id uuid) IS 'Admin-gated snapshot behind the product Groups panel: groups with their gedus and active members, the unassigned actives, and the waitlist in derived (waitlisted_at, id) order. Every participation object carries the same fields, including the two the panel''s refusal dialogs are keyed to: has_live_subscription (a real read on ALL THREE branches — a LEFT JOIN to family_subscriptions excluding status ''cancelled'', so it means live rather than ever-existed) and has_payment_marker (a real read of stripe_checkout_session_id — money once arrived for this seat, which demotion does not clear). Both are resolved here so the panel decides a drag from one snapshot rather than asking per chip. The person keys are participant_* (whoever holds the seat) and the contact behind a child''s seat is parent_first_name/parent_last_name; an adult seat names none of those and carries participant_email — its own address — instead. Each chip also carries participant_roblox_username/participant_roblox_user_id beside the Minecraft pair, so the panel can show whichever identity the product''s topic is about; the topic itself is NOT emitted here, because the page already holds the product row. All three branches also carry the staff-only flair — group_joined_at, note and note_updated_by_first_name — from one identical LEFT JOIN, which comes back NULL on the two group-less branches because that is the truth and because one expression is what keeps the three shapes one shape. The groups panel draws neither mark, and no admin surface reads either of them from THIS document — the group details page renders both and reads them off get_gedu_group_feed, the copy a note write invalidates — so all three fields ride here for shape parity across the three roster readers rather than for a reader of this one. All three branches also carry seat_offer_sent_at and seat_offer_expiry_notified_at, on exactly the same terms: only the WAITLIST branch can hold a non-NULL value (a CHECK forbids an offer stamp on any other status) and only the waitlist card reads them, but the expression is identical in all three so the shape stays one shape. Whether an offer is LIVE is derived on the reader''s side from sent_at plus the five-day window. Each entry of a group''s `gedus` carries the assignment `role` — primary or assistant — which is what the panel''s per-pill role select reads and writes back through apply_group_changes. This panel is the PERMANENT assignment editor; the session card''s staffing editor is a different tool, and nothing links the two, deliberately.';

COMMENT ON FUNCTION public.is_voice_group_member(p_group_id uuid) IS 'Who may be in this group''s voice room, and therefore — through is_chat_channel_member — in its in-call chat: an admin, an active seat-holder of the group, a gedu assigned to ANY group of the group''s product, or a live substitution dated TODAY OR YESTERDAY in the product''s timezone. The substitution arm is the one thing on this surface that is date-scoped rather than group-wide, and it is the narrower of the two deliberate exceptions to "a sub sees everything the main gedu sees": a sub belongs in the room on the date they are substituting and on no other date of the group. "The session in question" is evaluated at CALL TIME because the predicate is handed a group and nothing else. The substitution arm ADDS to the assignment arm and narrows nothing — a gedu assigned to the product keeps the product-wide mobility they already had. Total boolean; consumed by the voice_zones and chat policies, which is why it is granted to `authenticated` despite being a predicate. YESTERDAY counts beside today because a session dated Monday that runs past local midnight is still Monday''s session at 00:30 on Tuesday, and a session starting at 00:10 has its whole pre-window on the day before; asking about today alone ejects the substitute from the room and the chat at midnight, and refuses them before a small-hours start. The access window inside gedu_substitutes_session applies to whichever date matches.';

COMMENT ON FUNCTION public.is_voice_group_moderator(p_group_id uuid) IS 'Who MODERATES this group''s voice room and its chat: an admin, a gedu assigned to any group of the group''s product, or a live substitution dated today or yesterday in the product''s timezone. A POSITIVE allow-list, never an exclusion — the room learned that the expensive way, a "not a gamer" test having been one parent-seat release away from handing moderation to parents. Date-scoped on its substitution arm exactly as is_voice_group_member is, and for the same reason: the two move together, because the chat channel is gated by the pair and a substitute must be in the channel on their own date only. Its substitution arm spans the same two local dates as is_voice_group_member''s, and always must: the chat channel is gated by the pair, so a cross-midnight session where one of them dropped the substitute would keep them in the room with no moderation, or in the chat with none.';

COMMENT ON FUNCTION public.join_product_waitlist(p_product_id uuid, p_participant_id uuid, p_consented_documents text[]) IS 'Guarded, authenticated-facing entry point for joining a product waitlist. The customer is auth.uid(); the parent-of-gamer check and the required-consent gate both live in join_waitlist.';

COMMENT ON FUNCTION public.join_waitlist(p_product_id uuid, p_participant_id uuid, p_customer_id uuid, p_consented_documents text[]) IS 'Waitlist engine behind join_product_waitlist: gates the audience, refuses a product with the waitlist off, and either writes a waitlisted participation stamped with clock_timestamp() or returns the waitlisted/reserving/active row already there. Returns participation_id, waitlist_position (0 when the row already holds a seat rather than a place in line), status, and idempotent — false only on the call that ran the INSERT, true on a call that recognised an existing row. Anything that must happen exactly once per place in line (the confirmation email) keys on idempotent=false; the flag is the only way to tell a replay apart, since both answers are otherwise identical. It takes p_consented_documents and calls record_required_consents just below the idempotency return, so joining a queue is held to the same enrolment conditions as taking a seat — a family that could queue unconsented would first meet the conditions at promotion, which is the moment they are least able to decline — and a replay records nothing, because it is the same enrolment agreed once. It names p_customer_id as the acceptance''s accepted_by as well as its customer, the parent having ticked the boxes themselves. No EXECUTE grant to anyone: the guarded wrapper is the only caller.';

COMMENT ON FUNCTION public.offer_session_substitution(p_request_id uuid) IS '"Offer to substitute", from the gedu dashboard''s pool list. Guarded on gedu_may_substitute_session — certified, not the absent gedu, not already expected at that session, and holding no non-withdrawn request of their own on that (group, date) — plus the request being `open` and dated today or later in the product''s timezone. Idempotent on (request, gedu): offering twice is one offer. There is deliberately no ranking, no eligibility beyond certification, and no notification on any channel; the office decides, and auto-approving the first offer was rejected because the admin step IS the product. Returns the request document, which carries no offer_count for an offerer — who else volunteered is not their business. The document it returns CONCEALS the absent gedu: requested_by and requested_by_first_name arrive as JSON null, because otherwise offering would be a way to unmask the absent person on any pool row, leaving the pool''s own "names the session, never the person" rule one button-press deep.';

COMMENT ON FUNCTION public.promote_from_waitlist(p_participation_id uuid, p_group_id uuid) IS 'Admin-gated promotion of a waitlisted participation into a seat, under the product gate lock. No seat-count gate by design — promoting from a full waitlist is a deliberate capacity override. Clears waitlisted_at so the row leaves the queue ordering, and clears the two seat-offer stamps with it: an admin dragging an invited row is honouring that offer by hand, which ends it, and the emailed link stops validating on its own because it no longer matches the row. The clear is unconditional because the CHECK forbids an offer stamp on a non-waitlisted row.';

COMMENT ON FUNCTION public.record_attendance(p_group_id uuid, p_session_date date, p_participant_id uuid, p_status text) IS 'Record (or, with a NULL status, clear) ONE participant''s attendance mark for one session. Per-mark so concurrent writers cannot clobber each other; marks open at the session''s scheduled start (roll call during the session is the standard pattern) and never before. Open to an ADMIN or to the gedu assigned to the group — the guard admits either role and only the assignment half of the gate is skipped for an admin. The TARGET check is not: both callers must aim the mark at somebody who actually holds an active seat in the group, and both are refused before the session starts. The target is whoever holds the seat — an adult is marked present exactly as a child is, with no branch for it.';

COMMENT ON FUNCTION public.record_registration_marketing_consent(p_customer_id uuid, p_granted boolean) IS 'Record the marketing answer given on the parent sign-up form — the state row and its event row together, in one transaction. The register route called PostgREST twice for this and two calls are two transactions, so a failed second write left marketing_consents asserting an answer that marketing_consent_events could not corroborate; on the one consent whose value is provable provenance, an opt-in nobody can evidence is worse than none. Deliberately narrower than set_marketing_consent: the consent type is hardcoded to school_of_gaming (ours is the only list asked for at registration — the partner''s is asked on products) and the source is hardcoded to `registration`, so neither can be forged through this function. The customer IS a parameter because no session exists yet, which is why the only EXECUTE grant is to service_role: a parameter naming the subject can be aimed at somebody, so nothing reachable may call it. It still tests that the named profile is a `customer`, which is the invariant assert_role gives the self-service writer, read from the only place available here. Appends an event only when the state actually MOVES, so a retried registration request records nothing twice — and a first explicit "no" IS a move, because an absent row means never asked.';

COMMENT ON FUNCTION public.record_required_consents(p_product_id uuid, p_customer_id uuid, p_participant_id uuid, p_accepted_by uuid, p_consented_documents text[]) IS 'The enrolment-consent gate, and the only writer of consent_acceptances. Loads the product''s required document slugs, refuses the enrolment with check_violation unless the caller''s array covers ALL of them (naming the missing ones), and then writes one acceptance row per REQUIRED slug at that slug''s CURRENT version — the row with the greatest created_at, resolved server-side and never supplied by a caller. A product requiring nothing is a no-op, including when slugs are sent anyway. Carries no EXECUTE grant for any role, because every caller is SECURITY DEFINER and already holds the privilege as the owner. An array containing a NULL element is refused before anything else happens, and the missing-set test is a two-valued NOT EXISTS rather than `NOT (r = ANY (...))`: the ANY form answers SQL NULL — which NOT turns into NULL, not true — whenever the array holds a NULL and nothing matches, so ARRAY[NULL] would pass the gate for every required document and record acceptances nobody had given. It takes p_accepted_by, the profile that PERFORMED the act, and has THREE callers: create_participation and join_waitlist pass their own p_customer_id, because the parent ticked the boxes themselves, and admin_enroll_participant passes the acting admin''s auth.uid() while leaving customer_id the family''s. These consents are NON-REVOCABLE enrolment conditions — see the consent_acceptances table comment.';

COMMENT ON FUNCTION public.respond_seat_offer(p_participation_id uuid, p_offer_sent_at timestamp with time zone, p_accept boolean) IS 'A family''s answer to a seat offer, under the product gate lock. Compare-and-swap on p_offer_sent_at against the stored stamp: every way an offer ends moves that value, so a used link, a stale tab and a superseded offer all come back ''stale'' with no revocation table anywhere. The five-day window is re-checked here rather than trusted from the token, because the in-app path (a parent pressing Accept in My SOG) carries no token. THE WINDOW BINDS ACCEPT ALONE. A DECLINE succeeds for as long as the row exists, late or not: the deadline is there to stop a seat being claimed after we have offered it elsewhere, and none of that reasoning reaches a family giving a place back. THE DECLINED RESULT CARRIES TWO FLAGS AND THEY ANSWER DIFFERENT QUESTIONS. within_window says the answer beat the deadline. already_notified says seat_offer_expiry_notified_at was set when we read it — read before the DELETE, because the DELETE takes the column with it and after that nothing can tell whether staff were ever told this offer went unanswered. The caller mails on within_window OR NOT already_notified, which skips the mail only where the no-response mail demonstrably went: expiry here is OBSERVED rather than scheduled, so an offer nobody looked at between its fifth day and a late answer was never reported, and treating lateness alone as proof of notification made staff learn less from an answer than from silence. The already_notified read is deliberately not locked against a concurrent sweep — this transaction holds the product gate lock, not the participation row — so the worst case is one duplicate staff mail, which is the recoverable direction. THE PRODUCT IS RE-CHECKED BY ID ON THE LOCK: a MISSING product answers ''stale'' and grants nothing. That is the boundary of this function''s grandfathering — the TERMS the offer went out on survive an admin''s edit (the billing mode is deliberately not re-read), but the product''s own existence is not a term, and the one fact an honoured invite always requires is that the product it names still exists. A product that has merely run out of dates is NOT guarded: it still exists and nothing has been withdrawn. Existence is the whole of the test, there being no stored status to refuse on — a product''s lifecycle is derived from its dates. That guard is also the one refusal that stays generic all the way out to the reader: every other ''stale'' resolves to ''used'' when the caller re-reads the row, and only a row still holding this exact live offer resolves to ''invalid''. ACCEPT activates the seat and places it in the product''s single group, resolved again at answer time — if the product no longer has exactly one group the seat is still granted and lands unassigned, because a placement question is ours and not a reason to withdraw an invitation. There is no seat-count gate, deliberately: the same capacity override promote_from_waitlist makes, with a stronger claim behind it, so a product that refilled while the family was deciding goes one over. DECLINE hard-deletes the row, matching leave_my_waitlist_spot, and returns the four identifiers the staff mail names because they cannot be read afterwards. No EXECUTE grant to authenticated: the public landing route has no session to guard on — the signed token is the authorization — and the in-app route establishes the parent''s ownership before calling.';

COMMENT ON FUNCTION public.set_gamer_photo_consent(p_gamer_id uuid, p_consent_type public.gamer_photo_consent_type, p_granted boolean, p_source text) IS 'The one writer of a gamer photo consent: the card on the gamer''s page under the parent''s My SOG and the product signup panel both call it, so the two paths cannot drift. Guard-first on assert_role(''customer''), which keeps out gamers — a child may never grant permission to photograph themselves, and the read policy letting them SEE the answer is the whole of their access — gedus, and ADMINS, the last deliberately: an admin editing another family''s answer about their own child is not a thing this platform does. Unlike its marketing twin the SUBJECT is a parameter, because a parent has several children and the answer is about one of them; the parameter is defended by a parent_gamer link from auth.uid() to the named gamer, checked before anything is written, and "not your child" and "no such gamer" are refused with the same 42501 so the function is not an oracle for which uuids are children here. Accepts only the `settings` and `enrolment` sources — there is no `registration`, because no gamer exists when a sign-up form is filled in. IDEMPOTENT AND HONEST ABOUT IT: submitting the state already on file succeeds and appends NO event, which matters more here than anywhere, because every enrolment writes every asked box whether or not the parent touched it. A first explicit "no" IS a change — an absent row means never asked, and only one of those two is a decision.';

COMMENT ON FUNCTION public.set_gedu_certified(p_gedu_id uuid, p_certified boolean) IS 'Certify or de-certify a game educator. Admin-only (guard-first on assert_admin), and it stamps certified_at / certified_by server-side so the audit trail cannot be forged — which is why gedu_profiles carries no write grant at all and this RPC is the only way in. Called from the admin user-detail page through the admin''s own session.';

COMMENT ON FUNCTION public.set_group_member_minecraft(p_participant_id uuid, p_minecraft_username text, p_minecraft_uuid text) IS 'Set a group member''s Minecraft username + resolved UUID, scoped to participants actively enrolled in a group the calling gedu teaches. The Mojang lookup happens in the calling route, so a successful edit lands verified. In practice this is always a child: an adult seat carries no linked game account and the roster row shows that slot empty by design. Open to an ADMIN as well as to the assigned gedu, guard-first on assert_role with the group question as a second 42501 — the same shape the session writers and the group feed take. The admin caller is the product page''s per-group GROUP DETAILS page, which renders the gedu workspace''s roster body unchanged, inline editor included; an admin already holds this exact edit on /admin/users/[id], so admitting them here aligns two surfaces on one action rather than granting a power. An admin passes the group half outright and is exempt from nothing else: the target must be a gamer (23514), and a customer or a gamer is refused on the first statement. The group half also admits a LIVE SUBSTITUTION: the participant sits in a group the caller holds a substitution on, window open and certification intact. A sub running the session is the person with the child in front of them and is exactly who can read a handle off their screen.';

COMMENT ON FUNCTION public.set_group_member_roblox(p_participant_id uuid, p_roblox_username text, p_roblox_user_id bigint) IS 'Set a group member''s Roblox username + resolved account id, scoped to participants actively enrolled in a group the calling gedu teaches. The Roblox twin of set_group_member_minecraft, and identical to it in every respect but the key''s type: Roblox''s id is an int64, so the account-id parameter is a DEFAULTed bigint rather than a text column carrying an '''' sentinel, and omitting it is how an unverified save is expressed. The Roblox lookup happens in the calling route (neither Roblox API is reachable from a browser), so an id arriving here was resolved server-side and its presence is the whole of "verified". Clearing the username clears the id with it. In practice the target is always a child: an adult seat carries no linked game account and the roster row shows that slot empty by design. Open to an ADMIN as well as to the assigned gedu, in the same shape as its Minecraft twin — the admin group details page renders one roster editor serving both platforms, so admitting one alone would ship a control that works on a Minecraft group and refuses on a Roblox one. An admin passes the group half outright and is exempt from nothing else. The group half also admits a LIVE SUBSTITUTION, in the same shape as its Minecraft twin, and for the same reason: one roster editor serves both platforms.';

COMMENT ON FUNCTION public.set_group_notes(p_group_id uuid, p_public_note text, p_gedu_note text) IS 'Write a group''s standing family-facing and gedu notes. Open to an ADMIN or to the gedu assigned to the group — the guard admits either role and only the assignment half of the gate is skipped for an admin. Last-write-wins.';

COMMENT ON FUNCTION public.set_group_session_notes(p_group_id uuid, p_session_date date, p_report text, p_gedu_note text) IS 'Write the family-facing report and the gedu note for one session, materializing the row if needed. Open to an ADMIN or to the gedu assigned to the group: the guard admits either role, and the assignment half of the gate is skipped for an admin only. Everything else binds both alike — an unscheduled date is refused with check_violation, and updated_by is stamped with the caller, so an admin''s edit is attributed to the admin. Last-write-wins.';

COMMENT ON FUNCTION public.set_product_required_consents(p_product_id uuid, p_slugs text[]) IS 'Replace the set of consent documents a product requires, admin-only and guard-first on assert_admin. The only writer of product_required_consents: that table carries no write grant for any Data API role, and this function is what create_product and update_product both call so the join table has exactly one door. NULL and an empty array both mean "requires nothing", which is how a requirement is cleared. An unknown slug is refused by the foreign key into consent_documents — the only validation needed, since admins are trusted and a bad slug is a broken deploy rather than an attack. A NULL *element* is refused with check_violation, and the replacing DELETE uses a two-valued NOT EXISTS rather than `NOT (document_slug = ANY (...))`, which matches no row at all once the array holds a NULL and would quietly turn the replacement into a merge.';

COMMENT ON FUNCTION public.set_site_notes(p_location_id uuid, p_public_note text, p_gedu_note text) IS 'Write a site''s shared family note and its gedu note. The venue ADDRESS is not a parameter and is never touched — it belongs to the location record and is an admin''s to edit through the location itself. Open to an ADMIN, or to a gedu who teaches on an in-person product at that site. Last-write-wins on the notes, across products. A live SUBSTITUTION on any group of an in-person product at that site passes the site half too. The arm is location-shaped like the assignment one beside it rather than borrowed from a group predicate, because the question this function asks is about a BUILDING. It is the owner''s "a sub sees everything the main gedu sees" applied to the lowest-risk write on the surface, which is one fewer special case than carving it out would be.';

COMMENT ON FUNCTION public.update_product(p_id uuid, p_billing_mode public.billing_mode, p_translations jsonb, p_topic public.product_topic, p_spoken_language_code public.spoken_language, p_is_remote boolean, p_timezone text, p_registration_opens_at timestamp with time zone, p_for_gamers boolean, p_for_parents boolean, p_min_age integer, p_max_age integer, p_is_visible boolean, p_waitlist_enabled boolean, p_location_id uuid, p_start_date date, p_end_date date, p_seat_count integer, p_schedule_slots jsonb, p_prices jsonb, p_primary_gedu_fee_cents integer, p_assistant_gedu_fee_cents integer, p_municipality_fee_cents integer, p_material_url text, p_tag public.product_tag, p_region_lock_country text, p_required_consent_slugs text[], p_requires_gamer_creations boolean, p_invoice_customer_id uuid) IS 'Admin-gated product edit: parent row plus wipe-and-replace of translations, schedule slots, prices, the staff-only material link and the set of consent documents enrolling on it requires, under the product gate lock. It also DELETES the product''s waitlist whenever the saved waitlist_enabled is false — the flag goes off by unticking it or by uncapping, and the groups panel draws its waitlist column only while it is on, so a surviving queue would be invisible to every affordance that could work it. Deletion rather than promotion: promoting would grant seats with no subscription behind them, while the edit itself opens seats, so a dropped family can simply sign up again. It is silent by owner decision — no confirmation, warning or email — and keyed to the flag''s value rather than to it changing, so a queue left stranded on a product whose waitlist is already off is cleared by the next save. One exception: a waitlisted row carrying a LIVE subscription (a family_subscriptions row with status <> ''cancelled'') is skipped, because the FK cascades and deleting it would orphan billing Stripe still runs. SECURITY DEFINER — participations grants authenticated no writes, so the delete cannot run as the caller; the assert_admin() first statement is what authorizes the whole function. It assigns for_gamers/for_parents, which are non-defaulted parameters precisely because this statement assigns every editable column on every call. It also assigns tag, whose parameter IS defaulted — null is a legal tag and no CHECK backstops it, so omission is the only expressible way to clear one, and the required-nullable wire schema is what keeps that deliberate. It assigns region_lock_country the same way, and that column is deliberately editable on a live product: the lock gates future enrolments only, is never re-run against a seat already held, and is enforced in the UI alone because a family''s location is self-attested. start_date is the one editable column omission CANNOT clear: it is NOT NULL, so an omitting caller is refused by the column rather than quietly blanking the date on a live product. It does NOT assign image_path and takes no p_image_path: that column is derived from image_id by trg_products_apply_image_path on this very UPDATE, so an assignment here would be overwritten a moment later. p_spoken_language_code is public.spoken_language, an enum rather than a text key into a reference table. p_required_consent_slugs replaces the requirement set through set_product_required_consents — NULL clears it, and past acceptances are never touched, because dropping a requirement changes what future enrolments must agree to and says nothing about what past ones did. It assigns requires_gamer_creations, whose parameter defaults FALSE rather than null because the column is NOT NULL — so an omitting caller unflags the product, the same "omission clears it" semantics tag has, kept deliberate by the required wire field. It assigns invoice_customer_id, the FENNOA CUSTOMER a municipality club is invoiced to — tag''s shape exactly, a defaulted parameter whose omission clears the link, kept deliberate by a required-nullable wire field, and editable for the club''s whole life because who buys a club can change between terms. It is per club and never derived from a location, and chk_products_invoice_customer_only_for_muni refuses one on any other product type.';

COMMENT ON FUNCTION public.validate_profile_spoken_languages() IS 'BEFORE INSERT OR UPDATE OF profiles.spoken_languages. Its only rule is that no language appears twice — public.spoken_language, the column''s own type, decides which values are legal. It stays a trigger rather than a CHECK on purpose: EXECUTE on a trigger function is checked when the trigger is created, so this never needs a grant to authenticated, and therefore never needs a classification in the authorization spine, for a rule no caller has any business invoking.';

COMMENT ON FUNCTION public.withdraw_session_substitution_offer(p_request_id uuid) IS 'Deletes the caller''s offer on one request — an offer nobody accepted is not a fact worth keeping, so there is no withdrawn state for one. Keyed on the REQUEST rather than the offer, because the button knows which request it is looking at and an offer id would be a second identifier for the caller''s one row. Refused only when the caller IS the approved substitute: taking back an offer somebody has already been staffed on is a new absence, which is request_session_substitution''s job. Withdrawing a LOSING offer on a request substituted by somebody else is allowed and changes nothing visible. A caller who holds no offer on the request is REFUSED (42501, the same answer an unknown id gets) rather than deleting nothing and being handed the document anyway: answering would make this the cheapest read on the surface, a lookup of who is away keyed by request id. The document it returns conceals the absent gedu, as offer_session_substitution''s does.';

COMMENT ON TABLE public.account_consent_acceptances IS 'One row per (account, document VERSION) the account holder has accepted — the ACCOUNT-level counterpart of consent_acceptances, which is per enrolment and whose participant_id and product_id are both NOT NULL because a row there conditions one seat. A row here conditions no seat: it is what the person agreed to when they opened the account. Written at registration and NEVER REVOKED — a row is a statement that an agreement happened at an instant, and a statement about the past cannot be un-made, so there is no revoked_at column and there must never be one (the revocable marketing and photo consents are a separate system). The versions live in the consent registry: consent_documents holds the identity, consent_document_versions one row per published revision, and the composite foreign key below is what stops a row naming a text nobody published. A LATER version is a fresh row rather than an edit, because accepting a revision is a new agreement and the old one still happened. INSERT-ONLY and insert-only from ONE place: no Data API role holds a write grant, and record_account_consents — service_role only — is the sole writer.';

COMMENT ON COLUMN public.consent_acceptances.accepted_by IS 'The profile that PERFORMED the consent act — a different question from customer_id, which names the adult the agreement binds. On both family paths they are the same id, because the parent ticked the boxes themselves; on the admin comp-enrolment path this is the acting admin''s own profile while customer_id stays the family''s, so a staff-made record can never be read back as a parent''s own click. Taken from auth.uid() on that path and from the enrolment in hand on the others — never from a caller argument. No cascade on the FK, matching products.created_by: the profile that made a legal record is part of it, so it cannot be hard-deleted while the record stands; the family''s own removal runs through customer_id, which does cascade.';

COMMENT ON TABLE public.consent_document_versions IS 'One row per published revision of a consent document. Rows arrive by MIGRATION only — no Data API role holds a write grant — because a version is a document that was published, not a value an app invents. The CURRENT version OF A SLUG is the row with the greatest created_at for that slug, the same derivation gedu_contract_versions uses, and that is the version an enrolment records. Publishing a new revision is therefore one INSERT and touches no product: existing acceptances go on naming the version that was live when they were made, which is the whole point of storing a version rather than a boolean.';

COMMENT ON TABLE public.gamer_consent_acceptances IS 'One row per (gamer, document VERSION) an adult has accepted ABOUT THAT CHILD — the third subject in the consent system, beside consent_acceptances (per enrolment) and account_consent_acceptances (per account). What it exists for is the guardian declaration: the statement that this specific child is the adult''s own or that the adult is their legal guardian, made at the moment the child''s account is created and recorded against the wording that was on screen. An account-level declaration could not answer that, because an account that adds a second child later never said anything about the second one. NEVER REVOKED — a row is a statement that something was declared at an instant, and a statement about the past cannot be un-made, so there is no revoked_at column and there must never be one (the revocable photo consents are a separate system). INSERT-ONLY BY INTENT, and written from one place: neither `authenticated` nor `anon` holds any write grant, so no browser session can write a row by any path — they hold SELECT, gated by the two policies to a linked parent and to admins. `service_role` holds the usual full set, as it does on every table here, and create_gamer — service_role only — is the sole intended writer, which is what makes the declaration and the child arrive in one transaction or not at all.';

COMMENT ON TABLE public.gamer_photo_consents IS 'The CURRENT answer to "may this child be photographed for this partner" — one row per (gamer, consent type), and the row a gedu reads before a shutter opens. The twin of marketing_consents with the subject changed from an adult''s mailbox to a child''s image, which is why it is keyed on the GAMER and not on the answering parent: two parents linked to one child are answering one question about one child, and a per-parent key would let them hold two answers with no rule for which one a photographer obeys. An ABSENT row and `granted = false` are treated identically by every surface — never asked and asked-and-declined both mean the child stays out of the photo — and the distinction survives only in the event log, where it is the difference between a decision and a silence. Deliberately not derived from gamer_photo_consent_events: a check made at the moment of taking a photograph must not fold a history, and the present tense must not depend on a log a retention policy could one day trim. REVOCABLE by construction, which is what keeps it out of the non-revocable enrolment-condition system. Written by set_gamer_photo_consent and by nothing else: no Data API role holds a write grant.';

COMMENT ON TABLE public.gamer_photo_consent_events IS 'APPEND-ONLY history: one row per CHANGE to a gamer photo consent, and the evidence behind whatever gamer_photo_consents currently says. Nothing updates or deletes a row here — no Data API role holds any write grant at all, and the only writer is set_gamer_photo_consent — because an event is a statement that something happened at an instant, and editing one would destroy the only thing the table is for. A repeat submission that changes nothing appends nothing, exactly as in marketing_consent_events. Rows carry NO unique constraint: granting, revoking and granting again is the ordinary life of a revocable consent, and those three rows are history rather than duplicates. Readable by ADMINS ALONE, which is narrower than the state table beside it — a gedu needs today''s answer to decide whether to raise a camera, and has no business in the history of a family''s deliberations.';

COMMENT ON COLUMN public.gamer_photo_consent_events.source IS 'Which surface the answer came from: `settings` (the card on the gamer''s page under the parent''s My SOG) or `enrolment` (the ask inside a product signup panel). There is deliberately NO `registration` value, which is the one place this CHECK differs from marketing_consent_events'': that source exists because a parent ticks a marketing box before their account exists, and no gamer exists at that moment for a photo consent to be about. A CHECK rather than an enum because the set is a list of our own surfaces, which move with the product rather than with the data model.';

COMMENT ON COLUMN public.gedu_profiles.certified IS 'Whether an admin has vouched for this educator. Gates three things and nothing else: group assignment (UI-only, because assignment is admin-driven), instant-voice-room moderation (server-side, because it is gedu-initiated), and OFFERING AND HOLDING A SESSION SUBSTITUTION (server-side, twice over: gedu_may_substitute_session refuses an uncertified offerer or sub, and gedu_substitutes_session re-checks this column on every access test, so de-certifying an educator ends a live substitution''s reach into the group mid-window rather than only barring the next one). It is the ONLY eligibility test the substitution pool applies — coverage area, language and schedule clash are deliberately follow-ups — which is why an uncertified gedu sees an empty "Sessions needing a substitute" list rather than a refusal. An uncertified gedu still has broad platform access by design. Distinct from profiles.email_verified_at, which is about an address rather than a person.';

COMMENT ON CONSTRAINT gedu_profiles_criminal_record_check_stamp_matches_flag ON public.gedu_profiles IS 'The criminal record check''s moment is non-NULL exactly when its flag is true. Relied on by two admin surfaces that read different halves of it — the dashboard''s certification queue ships only criminal_record_check_at and reads NULL as "no check", while the users list reads only criminal_record_check_passed — so a disagreeing row would have the two describing the same educator differently. Nothing reachable can write one without the other (no write grant on the table; one RPC sets both in a single statement), which is why this fires only against a migration, a backfill or a hand-run UPDATE, and why failing loudly there is the whole of its job. criminal_record_check_by is deliberately outside it: ON DELETE SET NULL means a departed admin leaves a recorded check without a name, and that is correct.';

COMMENT ON TABLE public.marketing_consents IS 'The CURRENT answer to "may we mail this parent about this thing" — one row per (customer, consent type), and the row every send reads. Deliberately not derived from marketing_consent_events: a send must not fold a history to learn whether it may run, and the present tense must not depend on a log a retention policy could one day trim. An ABSENT row means never asked or never answered, which is not the same as `granted = false` (a parent who said no) — both are "do not mail", and only one of them is a decision the parent made. Account-level on purpose: the subject of a marketing consent is a mailbox, and a mailbox belongs to one adult rather than to one seat, which is the exact inverse of consent_acceptances and its per-enrolment key. REVOCABLE by construction — that is what makes this a separate system from the non-revocable enrolment conditions, which mandate the split. Written by set_marketing_consent and by the register route''s service-role client and by nothing else: no Data API role holds a write grant.';

COMMENT ON COLUMN public.marketing_consents.customer_id IS 'The adult who holds the permission, always a profile with role `customer`. Gamers and gedus hold none — a child''s synthetic address reaches nobody, and a gedu''s relationship with us is a contract rather than a mailing list — and the RPC''s role guard is what enforces that rather than a CHECK, because a role is a mutable property of a profile and a CHECK would freeze it at insert time. ON DELETE CASCADE: a permission to mail somebody who no longer exists is not a record worth keeping, and the audit trail cascades with them for the same reason.';

COMMENT ON COLUMN public.participations.group_joined_at IS 'When this seat entered its CURRENT group. NULL when the seat holds no group, and only then: rows older than the column carry a value derived from their own signed_up_at, so "unknown" and "derived from signup" are not distinguishable on an old row. That cost is accepted because signup is a provable lower bound on the true join — a seat cannot enter a group of a product it does not hold — so a derived value can only ever understate how new a member is, and the newcomer badge is the column''s only consumer. A move between two groups of one product RESETS the stamp: the member is new to THAT group, which is the whole claim the newcomer badge makes, and it is also the floor the session register measures its expectations from. Stamped only by trg_participations_stamp_group_joined_at, which is the column''s only ongoing writer — no RPC and no policy-driven UPDATE sets it, because group_id has at least five writers (including the ON DELETE SET NULL cascade from product_groups) and a trigger is the only point that sees all of them.';

COMMENT ON COLUMN public.products.image_path IS 'The object key every reader paints. DERIVED, with no exceptions: trg_products_apply_image_path writes the linked entry''s path on every products write and NULLs the column whenever image_id is NULL, so an app-supplied value is always inert and this column has exactly one writer. It deliberately carries NO foreign key into product_images(path): a second relationship between these two tables makes every PostgREST embed of product_images ambiguous (PGRST201) unless every caller hints it, and the trigger already guarantees what such a key would check.';

COMMENT ON COLUMN public.products.for_gamers IS 'Children may occupy a seat on this product. Default true, so a product is open to children unless somebody says otherwise.';

COMMENT ON COLUMN public.products.tag IS 'Optional design tag, NULL meaning untagged. Untagged is the ordinary state and renders nothing anywhere — no chip on the card, no chip on the detail hero, no explanation block — exactly as a gamers-only audience renders no badge. There is no default: a product is untagged until somebody says otherwise.';

COMMENT ON COLUMN public.products.region_lock_country IS 'Optional ISO 3166-1 alpha-2 country code this product is locked to; NULL means not locked, and is the ordinary case. ENFORCEMENT IS UI-ONLY BY DESIGN: nothing in this database refuses a participation on a locked product. A family''s location is self-attested and editable by them at any time, so a server-side block would check a value the blocked party can rewrite — an obstacle, never a guarantee. The shop''s signup panel reads this column and tells a parent outside the country that the product is not for them; that is the whole mechanism. Two accepted consequences: a determined parent can restate their location and enrol, and a parent who moves after enroling keeps their seat, because the lock gates the enrolment decision and is never re-run against an existing one. The CHECK constrains the shape only (two uppercase letters). WHICH countries may be chosen is the seeded half of SUPPORTED_COUNTRIES in the application config, enforced by the write contract and the admin picker, because that list changes as location rows are seeded and an enum here would both need a migration per country and turn an already-stored lock into a violation the day one is un-seeded. Unrelated to the municipality-club country binding, which constrains a muni club''s location pickers and says nothing about who may enrol.';

COMMENT ON TABLE public.product_gamer_photo_consents IS 'The admin-picked set: which photo consents a product''s signup panel ASKS a parent about, and — downstream — which products show a gedu the roster''s photo permissions at all. Empty for almost every product; the Roblox Programme delivered with Lynx Educate is what this exists for. A row here is an ask and never a requirement: declining is a complete answer and the seat is unaffected, which is the whole line between this table and product_required_consents. The question is PUT only when the selected participant is a gamer — a parent taking an adult seat is not a subject this consent can have. Written only by admin_set_product_gamer_photo_consents; no Data API role holds a write grant, so the join table has exactly one writer. Readable through the product''s own read predicate, exactly as product_prices, schedule_slots, product_required_consents and product_marketing_consents are, because the shop has to tell a stranger what signing up would ask them. ON DELETE CASCADE from products: an ask is a property of a product and means nothing without it.';

COMMENT ON TABLE public.product_marketing_consents IS 'The admin-picked set: which marketing consents a product''s signup panel ASKS a parent about. Empty for almost every product — the Lynx Educate partnership is what this exists for. A row here is an ask and never a requirement: declining is a complete answer and the seat is unaffected, which is the whole line between this table and product_required_consents. Written only by admin_set_product_marketing_consents; no Data API role holds a write grant, so the join table has exactly one writer. Readable through the product''s own read predicate, exactly as product_prices, schedule_slots and product_required_consents are, because the shop has to tell a stranger what signing up would ask them. ON DELETE CASCADE from products: an ask is a property of a product and means nothing without it.';

COMMENT ON COLUMN public.roblox_accounts.roblox_user_id IS 'Roblox''s int64 account id, present only when a lookup confirmed the account — its presence is the whole of "verified". Deliberately NOT unique: siblings sharing one Roblox account across two Sogverse accounts is supported, exactly as it is for Minecraft.';

COMMENT ON TYPE public.gamer_photo_consent_type IS 'The photo permissions a parent can hold on behalf of a gamer. One value, lynx_educate: School of Gaming does not use children''s photographs on its own products and so does not ask, and the Roblox Programme delivered with Lynx Educate is the one place real photographs of children arise. Named for the PARTY exactly as marketing_consent_type is, rather than for the activity — because an enum value here is a standing permission over a child''s image and, like a marketing consent and unlike a consent DOCUMENT, it has no text to version and no republication for a stored row to outlive. A future partner is a new value and a new sentence; what the enum buys is that a typo cannot become a permission nobody can find to revoke.';

COMMENT ON TYPE public.marketing_consent_type IS 'The marketing permissions a parent can hold. school_of_gaming is our own mailing list, asked for at parent registration. lynx_educate is our partner''s, asked for only on products an admin has attached it to — see product_marketing_consents. An enum rather than a whitelist table because a marketing consent, unlike a consent DOCUMENT, has no text to version and no republication for a stored row to outlive: it is a standing permission to mail, and the party it names is the whole of it.';

COMMENT ON TYPE public.participation_status IS 'Participation lifecycle. ''reserving'' is RETIRED (2026-08): paid participations are created at payment confirmation, so nothing writes it. PostgreSQL cannot drop an enum value, hence it remains listed.';

COMMENT ON VIEW public.user_list_entries IS 'One row per entry of the admin user list: every non-gamer profile, plus any gamer with no parent link. A linked gamer is not a row — it rides inside its parent''s linked_gamers array, so the parent-child collapse happens in the database rather than in the browser from two whole-table reads. Carries every profiles column, the two gedu standing flags, the children as JSON, and a family-wide search blob, so a page of 25 rows is one request and nothing a row renders needs a keyed follow-up read. SECURITY INVOKER, so RLS on profiles, parent_gamer, gamer_profiles, gedu_profiles, minecraft_accounts and roblox_accounts governs it exactly as a direct read of those tables would — which also means a role granted SELECT here must hold SELECT on all six, and that a caller who cannot see a link sees the child as a top-level row rather than as somebody''s. The FROM names one table and every derived value is a scalar subquery on purpose: that is what lets the newest page be an ordered index scan under a LIMIT, evaluating the children and the blob for the 25 rows it returns rather than for the whole table, and what lets an exact count skip them entirely. A join in the FROM would cost both.';


-- The migration proves itself, here and on every database it is applied to: no
-- comment on an object of ours in `public` still cites a migration number.
--
-- pg_description holds OBJECT comments and nothing else, so an inline `--`
-- comment inside a function body is neither visible to this query nor claimed
-- by it. The pattern is the unit test's, character for character: a leading
-- boundary and "not another digit", so that a citation written as a whole file
-- name (`00061_something.sql`) is caught as well as a bare number.
DO $$
DECLARE
  v_offenders text;
BEGIN
  SELECT string_agg(format('%s %s: %s', d.classoid::regclass, d.objoid, left(d.description, 160)), E'\n' ORDER BY d.classoid, d.objoid, d.objsubid)
    INTO v_offenders
  FROM pg_description d
  WHERE d.description ~ '(?<![0-9A-Za-z])00[0-9]{3}(?![0-9])'
    AND (
      (d.classoid = 'pg_class'::regclass AND EXISTS (
        SELECT 1 FROM pg_class c JOIN pg_namespace n ON n.oid = c.relnamespace
        WHERE c.oid = d.objoid AND n.nspname = 'public'))
      OR (d.classoid = 'pg_proc'::regclass AND EXISTS (
        SELECT 1 FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace
        WHERE p.oid = d.objoid AND n.nspname = 'public'))
      OR (d.classoid = 'pg_type'::regclass AND EXISTS (
        SELECT 1 FROM pg_type t JOIN pg_namespace n ON n.oid = t.typnamespace
        WHERE t.oid = d.objoid AND n.nspname = 'public'))
      OR (d.classoid = 'pg_constraint'::regclass AND EXISTS (
        SELECT 1 FROM pg_constraint k JOIN pg_namespace n ON n.oid = k.connamespace
        WHERE k.oid = d.objoid AND n.nspname = 'public'))
      OR (d.classoid = 'pg_trigger'::regclass AND EXISTS (
        SELECT 1 FROM pg_trigger g JOIN pg_class c ON c.oid = g.tgrelid
        JOIN pg_namespace n ON n.oid = c.relnamespace
        WHERE g.oid = d.objoid AND n.nspname = 'public'))
    );

  IF v_offenders IS NOT NULL THEN
    RAISE EXCEPTION 'object comments in public still cite a migration number:%', E'\n' || v_offenders;
  END IF;
END;
$$;
