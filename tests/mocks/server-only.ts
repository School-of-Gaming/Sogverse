// Empty stub for the `server-only` package, used as a Vite alias in
// vitest.config.ts. Next.js routes import "server-only" to fail-fast when
// imported into client bundles; under vitest there's no client bundle so
// the import is harmless.
export {};
